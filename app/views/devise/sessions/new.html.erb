<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-form navbar-left">
      <img class="logo" src="<%= image_url "qoverflow_logo.png" %>">
    </div>
    <form class="navbar-form navbar-right" role="search">

        <a class="btn btn-default botones-a botones-b" role="button" href="<%= root_path %>">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>  Volver</a>

    </form>
  </div>
</nav>
<div class="row">
<div class="col-md-4 col-md-offset-4 centrado">
  <div class="estilo_title centrado"><h2>Ingresar</h2></div>
<div class="errors">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <% flash.each do |name, msg| %>

                      
                      <% if msg.class == Array %>
                        <% msg.each do |message| %>
                          <%= content_tag :div, message, :id => "flash_#{name}" %>
                        <% end %>
                      <% else %>

                       
                        <%= content_tag :div, msg, :id => "flash_#{name}" %>

                      <% end %> 
  <% end %>
   </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions centrado">
    <%= f.submit "Entrar" , class: "btn btn-primary"%>
  </div>
  <div class="blankspace2"></div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
<div class="blankspace"></div>
<nav class="navbar navbar-default navbar-fixed-bottom">
  <div class="piecito">
    <p>____________________________________________</p>
  <p><a href="http://www.monkodev.com" target="_blank"><img class="logofondo" src="<%= image_url "monkodevlogo.png" %>"> Powered by Monkodev.com</a></p>
  </div>
</nav>
